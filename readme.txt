1, python + flask + nigix
2, python + flask + react
3, python + flask + vue
4, python + flask + angular
5, python + django

python3 app.py

nigix：
/usr/local/Cellar/nginx/1.23.4
/usr/local/etc/nginx/

起動：
sudo nginx
sudo nginx -s stop

起動：
launchctl load com.example.myapp.plist
launchctl start com.example.myapp
launchctl stop com.example.myapp
launchctl unload com.example.myapp.plist
http://127.0.0.1:9000/

python3：
/usr/bin/python3

Heroku cli ：
brew tap heroku/brew && brew install heroku
pip3 freeze > requirements.txt
heroku installed to /usr/local/bin/heroku

git init
git add .
git commit -m "first commit"

heroku create pyflask
git remote -v

npm run eject
webpack.config.js　修正必要です
path: path.resolve(__dirname, '../../../server', 'static'),


